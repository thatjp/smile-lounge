import React from 'react';
import PropTypes from 'prop-types';

const Documents = () => (
  <div>
    <h1>
      This will be a documents page
    </h1>
  </div>
);

// Documents.propTypes = {

// };

export default Documents;
